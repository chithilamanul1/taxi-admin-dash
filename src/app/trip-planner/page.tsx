import TripPlannerClient from '@/components/TripPlannerClient';

export const metadata = {
    title: 'AI Trip Planner - Airport Taxis Tours',
    description: 'Plan your custom Sri Lankan holiday with our AI-powered travel assistant.',
};

export default function TripPlannerPage() {
    return <TripPlannerClient />;
}
