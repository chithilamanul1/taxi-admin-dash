import TripPlannerClient from '@/components/TripPlannerClient';

export const metadata = {
  title: 'AI Trip Planner | Airport Taxi Tours',
  description: 'Plan your perfect Sri Lankan holiday with our AI-powered trip planner. Generate custom itineraries instantly based on your preferences.',
};

export default function TripPlannerPage() {
  return <TripPlannerClient />;
}
